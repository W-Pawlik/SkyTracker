import { Typography } from "@mui/material";
import { Box } from "@mui/system";

export const AccountSettingsView = () => (
  <Box>
    <Typography variant="h1">AccountSettings View</Typography>
  </Box>
);
